{
  "name": "agent",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "langgraphjs dev --port 2024",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@langchain/langgraph": "^0.2.0",
    "@langchain/langgraph-checkpoint-postgres": "^0.0.4",
    "@langchain/core": "^0.3.0",
    "@langchain/openai": "^0.3.0",
    "@lessonbuild/shared": "workspace:*",
    "@lessonbuild/db": "workspace:*",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@langchain/langgraph-cli": "^0.0.10",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
